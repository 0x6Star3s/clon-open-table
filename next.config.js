/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: true,
    images: {
        domains: ['images.unsplash.com', "resizer.otstatic.com"],
    },

}

module.exports = nextConfig
